<% if true or @can[:edit] %>
    <% @issues_id = params[:ids] %>
    <li class="folder">
      <a href="<%= issues_reports_path(issue_ids: @issues_id)%>" class="icon icon-report" target="_blank">Print Report</a>
    </li>
<% end %>
